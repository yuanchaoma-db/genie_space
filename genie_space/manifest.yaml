version: 1
name: "data-chat-app"
description: "Chatting with Databricks data using Genie API"

resource_specs:
  - name: "serving_endpoint"
    description: "Serving endpoint for the data chat app"
    serving_endpoint_spec:
      permission: "CAN_QUERY"
user_api_scopes:
  - serving.serving-endpoints
  - dashboards.genie
  - files.files
  - sql
